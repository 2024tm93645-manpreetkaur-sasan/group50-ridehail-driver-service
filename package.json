{
  "name": "driver-service",
  "version": "1.0.0",
  "description": "Driver microservice (Node.js + Express + Postgres) with JSON logs, correlation IDs, and Prometheus metrics.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "echo \"(add eslint if needed)\"",
    "test": "echo \"(add tests)\""
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "pino": "^9.4.0",
    "pino-http": "^10.3.0",
    "prom-client": "^15.1.2",
    "uuid": "^9.0.1"
  }
}
